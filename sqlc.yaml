version: "2"

sql:
  - engine: "postgresql"
    queries: "pkg/service/dmls.sql"
    schema: "pkg/service/ddls.sql"
    rules:
      - sqlc/db-prepare
    gen:
      go:
        package: "data"
        out: "pkg/service/data"
        sql_package: "database/sql"
        initialisms: []
        emit_interface: true
        emit_empty_slices: true
        emit_json_tags: true
        json_tags_case_style: "camel"
